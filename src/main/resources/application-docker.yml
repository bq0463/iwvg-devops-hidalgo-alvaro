server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres-server:5432/iwvgdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

